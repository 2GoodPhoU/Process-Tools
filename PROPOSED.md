# Proposed — Process-Tools

> Things scheduled runs want to do but need human approval before execution. Triaged in the evening review window. Approved items graduate to QUEUE.md by the next Planner run.

## How approval works

- Check the box `[x]` to approve. The next Planner run will move it to QUEUE.md with appropriate priority.
- Strike through (or delete) to reject.
- Annotate with `[needs-discussion]` if you want to talk it through before deciding.

## Format

```
- [ ] [proposed-by: role / YYYY-MM-DD] Title
  - Why: ...
  - What it would do: ...
  - Risk / blast radius: ...
  - Suggested priority: P0 | P1 | P2
```

---

- [ ] [proposed-by: night-auditor / 2026-04-29] Commit (or stash) the ~2-day-old uncommitted patch line in `requirements-extractor/` before any worker touches it.
  - Why: working tree contains 13 modified tracked files (~840 LOC diff against HEAD: CLAUDE.md, QUEUE.md, STATE.md, schedule.json, the four sample bpmn-validation outputs, requirements-extractor's CHANGELOG and four core modules) plus 12 untracked items (~1933 LOC: `compound.py`, `multi_action.py`, four new test files, `PATCH-0.6.1-NOTES.md`, `PATCH-0.6.2-NOTES.md`, two new sample-procedures dirs, a `tests/baselines/` dir, and `build.log`). Last commit was 18ee238 on 2026-04-27 — automation went live 2026-04-28 and the baseline-pytest queue item explicitly captures "known-good" against today's working tree. With models.py in a syntax-error state (see today's P0), the patch line is on top of broken code; capturing a baseline against it pollutes the marker.
  - What it would do: have Eric review the 0.6.1/0.6.2 patch notes, repair the models.py truncation, run the full suite, and either (a) commit the patch line as one or more focused commits, or (b) stash it so the baseline runs against 0.6.0 (commit 18ee238). Either is fine — the point is the working tree should be a deliberate state before workers consume it.
  - Risk / blast radius: low if done now; rising. Each automated worker run that hits a broken working tree adds a JOURNAL entry and blocks the queue without producing useful output.
  - Suggested priority: P1

- [ ] [proposed-by: night-auditor / 2026-04-29] Fix the Windows pre-commit hook's Python discovery so future commits can't bypass the truncation guard.
  - Why: the most recent commit (a07224b) was made with `--no-verify` because the pre-commit hook picks up the MS Store Python stub instead of pyenv. The hook's `py_compile` check is exactly what would have caught the models.py truncation now sitting uncommitted in the working tree. Until the hook works on Eric's Windows setup, the documented "load-bearing layer against edit-tool truncation" is off.
  - What it would do: make `scripts/pre-commit-check.sh` robust to Windows Python discovery — e.g. honor `PYTHON` env var (it already does — but the hook needs to be invoked with it set), prefer `py -3` on Windows over bare `python3`, or add a `pyenv shims` lookup. Re-validate by re-enabling the hook and committing a known-clean file.
  - Risk / blast radius: small. Bash script change only; no shipped-binary impact.
  - Suggested priority: P2

- [ ] [proposed-by: night-auditor / 2026-04-29] Make `scripts/test_all.sh` fail-loud on test-module load errors.
  - Why: tonight's audit caught 27 `unittest.loader._FailedTest` ERRORs in requirements-extractor — but the per-tool `tail -3` summary only shows the final "Ran N tests / FAILED (failures=5, errors=27)" line, and the workshop-wide summary reports "Total ran: 205 tests" without mentioning that 27 modules never loaded. A future run with, say, only load-errors and 0 numeric failures could pass the eyeball test even though half the suite never executed.
  - What it would do: extend the suite-level summary to print error+failure counts alongside ran-counts, and make the workshop-wide summary line surface aggregate error counts (not just suite pass/fail). Optional: add a "modules failed to load" count parsed from the unittest output.
  - Risk / blast radius: scripts only; no source-tree or shipped-binary impact. ~20 LOC of bash.
  - Suggested priority: P2

- [ ] [proposed-by: researcher / 2026-04-29] Fold the Camunda Modeler import checklist into `samples/bpmn_validation/README.md`.
  - Why: the README's current "How to validate" section says "Verify visually: two lanes, four tasks, no errors at import" — three lines. The 2026-04-29 research pass found the import-failure surface is more specific than that (which lint warnings are platform-tag noise vs. real problems, what to expect on Camunda's auto-rewrite during save, what a structural-not-byte round-trip diff means in practice). The checklist exists in `research/2026-04-29-camunda-import-checklist.md` sections 2 and 6 — it should live in the README the validator actually reads, not in a research file they may or may not find.
  - What it would do: replace the current 3-bullet "Verify visually" list with the section-2 table (pool / lanes / tasks / events / flows / gateways / annotations + pass criterion per row) and append the section-6 procedure (open in Modeler, walk the table, repeat in bpmn.io, re-save and structural-diff, document outcome in DECISIONS.md). Keep the file ≤2 pages — anti-sprawl rule.
  - Risk / blast radius: doc-only. No source-tree or shipped-binary impact.
  - Suggested priority: P2

- [ ] [proposed-by: researcher / 2026-04-29] Add a small structural-diff helper for BPMN before/after Camunda re-save.
  - Why: the validator step "open in Camunda, save, confirm structural identity" is currently eyeball work. Camunda reformats XML on save (attribute ordering, indent), so a byte diff is useless; the meaningful diff is at (element-id, parent-lane, sequence-flow source/target, BPMNShape coverage) level. A 50-line stdlib script would make this repeatable and put it in CI for future emitter changes.
  - What it would do: small Python script that takes two `.bpmn` files and prints a structured diff: missing/added ids, lane-membership changes, sequence-flow source/target deltas, flow-node-without-shape count. No third-party deps (stdlib `xml.etree.ElementTree`).
  - Risk / blast radius: new file under `nimbus-skeleton/scripts/` or a new top-level utility — touches the orphan-dirs constraint, so the call has to wait until the orphan-dirs decision lands.
  - Suggested priority: P2

- [ ] [proposed-by: worker-8am / 2026-04-29] Broaden the truncation/NUL-byte sweep to actually catch trailing NUL bytes on tracked Python files.
  - Why: today's P0 had two distinct corruption modes sitting in the working tree, not one. The QUEUE definition-of-done called out only the duplicated-block + truncated docstring. The OTHER hazard — 192 trailing NUL bytes at EOF (rendered in `git diff` as one whitespace line followed by `\ No newline at end of file`) — was invisible to the Read tool (which silently filters NULs) and survived the night-auditor's "NUL-byte sweep clean" line. The cascade made `python3 -m py_compile` fail with `SyntaxError: unterminated triple-quoted string literal` (which the auditor saw) AND with `ValueError: source code string cannot contain null bytes` (which only surfaced after the duplicate block was removed). Without a NUL-aware check, future runs will keep losing time to this same shape.
  - What it would do: add a `grep -lP '\x00' <files>` pass (or equivalent — Python `open(rb).count(b'\x00')` is portable) to `scripts/pre-commit-check.sh` so the hook fails on any tracked `.py` file that contains a NUL byte. Mirror the same check in the night-auditor's audit pass so the audit doesn't claim "NUL-byte sweep clean" when grep's default behavior is treating the file as binary. Optional: extend the worker-side bailout in `roles/worker.md` so a worker that hits a NUL-byte read knows to fall back to a binary-mode rewrite rather than re-running Edit.
  - Risk / blast radius: scripts + role doc. No shipped-binary impact. ~30 LOC bash + a paragraph in `roles/worker.md`.
  - Suggested priority: P2

- [ ] [proposed-by: night-auditor / 2026-04-29] Refresh the test-count claims in CLAUDE.md to match reality.
  - Why: CLAUDE.md says "pytest (~508 tests in requirements-extractor + 33 in nimbus-skeleton + 13 in process-tools-common)". Tonight's discovery: nimbus-skeleton runs 40, process-tools-common runs 26, compliance-matrix runs 30 (uncited). requirements-extractor's 508 figure is unverifiable until the syntax error is fixed (109 ran tonight, 27 modules failed to load). Counts also drifted because new tests were added in the uncommitted 0.6.1/0.6.2 patch line. Also: the script uses `unittest discover`, not pytest — minor wording inconsistency.
  - What it would do: after the models.py P0 is fixed and the patch line is committed/stashed, re-run the suite and update the counts in CLAUDE.md. While there, change "pytest (~N tests)" to "unittest discover" to match the actual harness.
  - Risk / blast radius: doc-only.
  - Suggested priority: P2

- [ ] [proposed-by: night-auditor / 2026-04-30] Refresh `.git/index` from HEAD so `git status` is no longer chronically out-of-sync with reality.
  - Why: `git status -s` currently shows `MM` markers on `DONE.md`, `JOURNAL.md`, `QUEUE.md` (different blobs in HEAD, index, and working tree) and `D ??` markers on `requirements-extractor/BASELINE-2026-04-29.md` and `requirements-extractor/DECISIONS.md` (staged-for-deletion in index but present on disk). HEAD has the correct content (verified via `git ls-tree -r HEAD` — both files exist with non-empty blobs). The on-disk content for DONE.md, QUEUE.md, BASELINE, and DECISIONS is byte-identical to their HEAD blobs (md5 matches). What's wrong is `.git/index`: the worker-9am, worker-10am, worker-11am, worker-12pm runs all committed via `GIT_INDEX_FILE=/tmp/...` plumbing-path workaround, which writes commits but never updates the local `.git/index`. The index is now stuck at a pre-worker-9am state. Every future Worker run that does normal `git diff` / `git add` will see ghost diffs and risk re-committing already-committed content (or losing it).
  - What it would do: from a non-locked git state (i.e., once `.git/index.lock` is gone), run `git read-tree HEAD` (or equivalently `git reset --mixed HEAD` — does NOT touch the working tree) to refresh the index from HEAD. After that, `git status` should show only the legitimate working-tree edits (the parser/config/CHANGELOG/CLAUDE/STATE/roles/sample-bpmn modifications, plus the JOURNAL append, plus the untracked items). No code change. No history rewrite.
  - Risk / blast radius: low. Read-tree only changes `.git/index`; working-tree files are untouched. The only "loss" is the stale index entries that are wrong anyway. Verify with `wc -c <file>` against `git cat-file -s HEAD:<file>` before and after — every file's working-tree size should be unchanged.
  - Suggested priority: P1 (the morning Workers will keep tripping over this until it's resolved).

- [ ] [proposed-by: night-auditor / 2026-04-30] The Read tool surfaces a different version of files than bash/Python sees on disk. Document the workaround as a project-wide rule.
  - Why: the night-audit found 5 files (CLAUDE.md, STATE.md, roles/{night-auditor,planner,worker}.md) where the Read tool returns the full HEAD/index content (matching `git show HEAD:<file>`) while `wc -lc <file>`, `tail -c 200 <file>`, and `python3 open(p,'rb').read()` all return a SHORTER, mid-sentence-truncated content. Worker-10am noted this on JOURNAL.md yesterday and worker-11am/12pm both pre-emptively used python3 binary-mode appends instead of the Edit tool. Tonight's audit confirms the disconnect on five more files. Today's morning workers will silently rely on Read showing them complete role instructions; their bash-side checks (`git diff`, `git status`, `tail`) will see different content. That gap is how today's truncation hazard went unnoticed by the 9am-12pm workers despite being right in front of them.
  - What it would do: amend `roles/worker.md` (and the auditor/planner roles) with one line: `If you need the actual on-disk content of a file (for git diff, for grep, for any check), do NOT trust the Read tool — use bash/Python directly.` And conversely, when EDITING a file, do NOT trust the Edit tool's reported size/content; re-verify post-edit with `stat -c %s` and `tail -c <N>` from bash.
  - Risk / blast radius: doc only. ~3 lines added across 3 role files.
  - Suggested priority: P2.

- [ ] [proposed-by: worker-10am / 2026-04-30] Patch `DocumentDataExtractor.spec` to close the two shipping-now bundle gaps surfaced by today's PyInstaller spec audit.
  - Why: today's audit (see `requirements-extractor/DECISIONS.md` 2026-04-30 entry) found two real gaps that will fail at runtime on the offline target, not at build time. (1) `yaml` (PyYAML) is imported by `requirements_extractor/config.py:391` and `requirements_extractor/keywords_loader.py:100` but is not in the spec's `_bundle` list -- any user shipping a `*.reqx.yaml` config or YAML keywords file gets the project's own "Install with: pip install pyyaml" error, which is a hard wall on an air-gapped network. (2) `requirements_extractor.actor_heuristics` is reached only through a lazy `from .actor_heuristics import extract_actor_candidates` inside `actors.py:252` -- the exact dynamic-dispatch pattern the spec's own comment (lines 95-96) flags as "PyInstaller's static analysis sometimes misses," and the rule-based offline fallback that CLAUDE.md calls "the offline-network load-bearing layer."
  - What it would do: two-line patch to `requirements-extractor/packaging/DocumentDataExtractor.spec`. (a) Add `"yaml"` to the optional add-ons tuple at lines 76-81 alongside `pdfplumber`, `pdfminer`, `tkinterdnd2` -- preserving the existing best-effort `_bundle()` treatment so a CLI-only build venv without PyYAML installed isn't forced to drag it in. (b) Add `"requirements_extractor.actor_heuristics"` to the explicit internal hiddenimports list at lines 96-119, alphabetically between `actor_scan` and `actors`. Confirm the patched build via `pyinstaller packaging/DocumentDataExtractor.spec --clean --noconfirm` and a smoke-load of a YAML config + a forced `use_heuristics=True` actor extraction in the resulting binary on an offline network.
  - Risk / blast radius: low. Spec-only change. No source-tree edit; no test changes (the suite doesn't exercise the binary). Rebuild adds ~5 MB to the bundle (PyYAML's wheel is ~750 KB unzipped + its CExtensions). The actor_heuristics line is a no-op for any build where PyInstaller's static analysis already picked it up; it costs zero bytes if so, and saves a runtime ImportError on the offline target if not.
  - Companion item (do NOT do separately): when the auditor's 2026-04-29 PROPOSED commit-or-stash for the 0.6.1/0.6.2 patch line is approved, the same spec edit must add `"requirements_extractor.compound"` and `"requirements_extractor.multi_action"` to the explicit hiddenimports list. Today's audit found `parser.py:271` and `parser.py:275` import these as top-level static imports; PyInstaller will probably catch them, but spec policy says list them anyway. Bundle the two-line addition into the same commit that lands the patch line.
  - Suggested priority: P2 (real gap on the offline target, but no user has hit it yet -- gap surfaces only when a user ships a YAML config or trips the lazy actor_heuristics import path; the rule-based fallback is opt-in via `use_heuristics=True` per the audit findings).

- [ ] [proposed-by: cowork-session / 2026-04-30] Investigate the night-auditor role's confabulation of a non-existent push policy.
  - Why: the 2026-04-30 night-auditor PROPOSED entry on push-policy reconcile referenced a "(when un-truncated)" version of `roles/worker.md` containing an `automation/<role>-<YYYY-MM-DD>-<slot>` branch policy and an explicit ban on the plumbing-path workaround. Verified today: no such policy exists in `roles/worker.md` (HEAD or WT, identical at 2188 bytes), in CLAUDE.md, or in any other role file (`grep -E 'automation/|<role>-<YYYY|never push.*main' roles/*.md CLAUDE.md` -> 0 matches). The night-auditor invented policy text and then flagged a "violation" of it. This pattern -- audit reasoning over fabricated baseline -- is the same shape as the "NUL-byte sweep clean" false-clean from 2026-04-29. Audit quality cannot be relied on if the auditor synthesizes policy.
  - What it would do: figure out where the phantom text came from. Candidates: (a) a previous version of `roles/worker.md` that was rolled back (check `git log -p roles/worker.md` -- if so, the auditor is reading via Read tool which may surface stale blob content), (b) a parenthetical in another role doc that the auditor generalized, (c) pure confabulation. Once identified, either restore the policy intentionally OR add a check to the night-auditor role: "Quote policy text directly from the file via bash `cat`, not via Read tool, and never paraphrase." Optional: add a self-check step where the auditor greps the repo for any policy clause it cites before publishing the finding.
  - Risk / blast radius: investigation only at first. Doc-only fix at the end. No source-tree impact.
  - Suggested priority: P2 (no immediate harm done -- today's resolution costs nothing -- but recurrence will burn time on every false-flag).

- [ ] [proposed-by: night-auditor / 2026-05-01] Commit Eric's BPMN-waiver edit to `CLAUDE.md` before any future foundational-files repair touches the file.
  - Why: `git status` shows ` M CLAUDE.md` -- HEAD is 4660 bytes (4660-byte version without the "Task-specific waivers" section), disk is 5272 bytes (includes the +612-byte waiver block at lines 22-24 authorizing computer-use for Camunda Modeler 5.x and Chrome MCP for demo.bpmn.io, scoped to the BPMN P1 only). Worker-12pm flagged the uncommitted state in JOURNAL.md (12:00 entry) and the digest carried it forward at 16:45 ("Plus your CLAUDE.md waiver edit is uncommitted on disk -- worth committing tonight"). Two days running and it is still uncommitted. Risk: the established truncation-recovery procedure is `git show HEAD:CLAUDE.md > CLAUDE.md` (used by worker-8am 2026-04-30 to repair the prior truncation). If a future Worker run hits a truncation on CLAUDE.md and runs that recovery, the on-disk waiver disappears silently -- and any Worker that read the waiver before the recovery loses the policy authorization the next time the file is read. The waiver is also self-sunsetting ("Remove this waiver after the queue item closes") -- worth landing it as a real commit so the removal is also a real commit.
  - What it would do: stage and commit `CLAUDE.md` as a single bookkeeping commit titled `chore(claude): land BPMN/Camunda computer-use waiver`. The change is exactly the +612 bytes of the waiver block; no other content drifts. Either Eric does this in evening review, or tomorrow's morning chain leaves it alone (Workers per role spec do not commit Eric's working-tree edits). Document the waiver-removal commit in DECISIONS.md voice when the BPMN P1 closes.
  - Risk / blast radius: zero -- single-file bookkeeping commit, content already on disk, no behavior change. Removing the source-of-policy ambiguity is a small win; the persistent uncommitted state is the actual risk.
  - Suggested priority: P2 (compounds slowly; matters most when a CLAUDE.md repair-from-HEAD fires).

- [ ] [proposed-by: night-auditor / 2026-05-03] Restore-or-commit the byte-equal-but-content-different drift on `roles/planner.md` and `roles/worker.md`.
  - Why: researcher 2026-05-02 04:00 documented (and STATE.md "Known constraints" carries forward) that both files are byte-equal to HEAD (2102 / 2188 bytes -- so no `wc -c` parity check catches the drift) but content-different. `roles/planner.md` diffs 13 lines vs HEAD: HEAD has the canonical "What you DO NOT do" section + 5 bullets; disk replaces it with "Optional engineering skill hooks" + 1 truncated bullet ending mid-sentence on `\ No newline at end of file`. `roles/worker.md` diffs 42 lines vs HEAD: HEAD has the clean numbered steps + bailout conditions; disk inserts mandatory `engineering:testing-strategy` / `engineering:code-review` / `engineering:debug` skill-hook references and truncates step 6 mid-bullet ending on `\ No newline at end of file`. Both edits look like a partial cowork-session experiment with skill-hook integration that never got committed and never got reverted. Every Planner / Worker scheduled run since 2026-04-30 14:07 UTC has Read the on-disk version (~5 nights of automation). The on-disk version is also what JOURNAL entries consistently cite (e.g., worker-9am 2026-04-29 "engineering:debug" reference, worker-8am-12pm 2026-05-02 repeated `roles/worker.md` step-2 citations).
  - What it would do: Eric picks one of three. (a) **Restore-from-HEAD**: `git show HEAD:roles/planner.md > roles/planner.md` and the same for `roles/worker.md`. WT then matches HEAD; index is already synced (` M` not `MM`); `git status` clears the two ` M` markers without a commit needed. Use this if the drift is unintended. (b) **Commit-with-intent**: stage and commit both files as a single bookkeeping commit titled e.g. `chore(roles): land skill-hook integration draft`, accepting that the HEAD bullets get replaced with the truncated drafts. Use this if the cowork-session changes were deliberate but should be re-finished first (the truncations are visible). (c) **Re-finish-then-commit**: edit both files to add a clean closing bullet on the truncated sections, then commit. Highest-fidelity but takes ~10 minutes of attention.
  - Risk / blast radius: low and growing. Every Worker today operates against the on-disk text without knowing it diverges from HEAD; that is exactly the conditions that produced the night-auditor 2026-04-30 confabulation pattern (research/2026-05-01-night-auditor-confabulation.md). Single-file repair on (a); single bookkeeping commit on (b); a ~10-minute edit on (c). No source-tree impact in any path.
  - Suggested priority: P2 (chronic, low-bleed; matters most when a Worker quotes the on-disk version as policy in a JOURNAL entry and a future audit reasons over the discrepancy).

- [ ] [proposed-by: night-auditor / 2026-05-04] Empirical correction to the role-file content-drift PROPOSED entry above: it covers 3 files, not 2, and the bytes are NOT equal to HEAD.
  - Why: the prior auditor 2026-05-03 PROPOSED P2 (entry directly above) claims `roles/planner.md` (2102 bytes) and `roles/worker.md` (2188 bytes) are "byte-equal to HEAD ... so no `wc -c` parity check catches the drift." Tonight's audit re-measured every claim from disk via `wc -c`, `stat -c %s`, and `python3 open(rb).read()`-len (all three agreeing). Disk: `roles/night-auditor.md` 2477 / `roles/planner.md` 2682 / `roles/worker.md` 4126 bytes. HEAD blobs (`git cat-file -s HEAD:<file>`): 1830 / 2102 / 2188. Disk EXCEEDS HEAD by +647 / +580 / +1938 bytes; `wc -l` exceeds by +4 / +4 / +15 lines. So (a) the drift covers `roles/night-auditor.md` too, which the prior PROPOSED entirely missed; (b) `wc -c` DOES catch this drift on all three files -- the "byte-equal" claim was empirically false the moment it was written. mtime on all three role files is 2026-05-02 07:46 UTC (~46h ago at this audit), so the drift has been on disk in this exact shape since well before the prior auditor's run; the byte-equal claim was a misread, not a stale snapshot. STATE.md "Known constraints" carries the same incorrect numbers ("byte-equal to HEAD ... 2102 bytes ... 2188 bytes"); `research/2026-05-02-night-auditor-confabulation-sweep.md` is the upstream source per STATE.md "researcher 2026-05-02; re-verified by auditor 2026-05-03" -- worth checking whether it carries the wrong numbers too.
  - What it would do: this is not a separate action -- it is an empirical correction to and a scope extension of the prior PROPOSED entry (the three-options menu still applies: restore-from-HEAD / commit-with-intent / re-finish-then-commit), to be exercised on three files instead of two: `roles/{night-auditor,planner,worker}.md`. While there, fold the corrected byte/line numbers into STATE.md "Known constraints" and into `research/2026-05-02-night-auditor-confabulation-sweep.md` if it carries the wrong figures. The prior PROPOSED entry above is otherwise correct on everything else (intent of the drift, three-options framing, low-bleed risk profile) and should not be deleted -- just amended.
  - Risk / blast radius: same as the prior PROPOSED -- doc-only repair on (a), single bookkeeping commit on (b), ~10-15 minute attended edit on (c). No source-tree impact in any path.
  - Audit-quality note: this is the 4th confabulation-pattern occurrence in the running window (cowork-session PROPOSED 2026-04-30 + research/2026-05-01..2026-05-03). Same shape every time: an audit run reasoned over an unverified baseline (here: the size-matches-HEAD assumption) instead of the on-disk reality. The corrective discipline (STATE.md "Known constraints" carrying bash-verified output) is itself the load-bearing chain that broke this round -- the wrong numbers were carried forward through STATE.md and `research/2026-05-02-...` for ~3 days before this audit re-measured. Worth Eric's attention because the audit-layer-as-single-open-gap finding from researcher 2026-05-03 04:00 just got re-confirmed by tonight's audit.
  - Suggested priority: P2 (the underlying drift is unchanged in priority; the audit-quality dimension is the same chronic concern Eric is already triaging via the cowork-session 2026-04-30 PROPOSED).

- [ ] [proposed-by: cowork-session / 2026-05-04] Add a mandatory post-edit verification step to `roles/worker.md` (and planner.md) that catches truncation BEFORE commit, not after.
  - Why: edit-tool truncation has hit `models.py`, `QUEUE.md`, `JOURNAL.md`, and the 5 foundational role files (CLAUDE.md + 3 roles + STATE.md) over the past 7 days -- roughly 1 occurrence every 3-5 days. Every recovery costs a Worker shift. The current convention is reactive ("use python binary-mode write if Edit drops content"); the workflow has no preventive gate. Worker-8am 2026-04-30 fixed the 4-file foundational truncation via `git show HEAD:<file> > <file>` -- that recovery pattern is also what would silently strip Eric's CLAUDE.md waiver if it fires unprotected.
  - What it would do: amend `roles/worker.md` step 5 (and planner.md step 2) with: "After ANY Edit/Write call on a file >2KB, you MUST verify in this order: (a) `wc -c <file>` matches the byte count Edit reported, (b) `tail -c 80 <file>` ends on a complete sentence + newline, (c) for `.py` files: `python3 -m py_compile <file>` exits 0, (d) for tracked files where the edit was content-equivalent-to-HEAD, `diff <(cat <file>) <(git show HEAD:<file>)` is empty. If ANY of (a)-(d) fails unexpectedly, do NOT commit -- bail to NEEDS-INPUT and use python binary-mode rewrite to repair." Same for planner overwrites. ~6 lines of role-doc text; replaces the post-hoc recovery loop with a pre-commit guard.
  - Risk / blast radius: doc only. Adds ~30 seconds per Edit-touching Worker shift. No code changes, no schedule changes. The hazard CLAUDE.md flags becomes a workflow gate instead of a folklore.
  - Suggested priority: P1.

- [ ] [proposed-by: cowork-session / 2026-05-04] Split QUEUE.md into `## Auto` and `## Manual Gate` lanes; Workers consume only `Auto`, planner empty-queue detection ignores `Manual Gate`.
  - Why: the BPMN P1 has been the only QUEUE item AND attended-only for ~67h. Result: 8+ consecutive empty-queue Worker shifts because the role spec correctly says "skip [in-progress] attended-only" but then there's nothing else to do. Same structural shape will recur the next time any GUI/cross-system/computer-use item lands (and the GitHub MCP integration is one such candidate). The fix isn't to make Workers smarter about request_access -- it's to acknowledge that a manual-gate item is structurally NOT in the unattended-Worker queue, while still being trackable.
  - What it would do: amend QUEUE.md format to two sections -- `## Auto` (default; Workers scan top-down) and `## Manual Gate` (items requiring human attendance, computer-use approval, or out-of-band action; each item carries a "Wakes when: <condition>" note). Workers in step 2 read ONLY the `Auto` section. Planner's "is QUEUE empty for unattended Workers?" check operates on `Auto` count, not total. Manual Gate items still appear in digests (Waiting on you), still get triaged, but don't trip the empty-queue NEEDS-INPUT cycle. The BPMN P1 + the future GitHub-MCP-integration fall under Manual Gate cleanly.
  - Risk / blast radius: format change to QUEUE.md + ~4 lines in `roles/worker.md` step 2 + ~2 lines in `roles/planner.md` step 3 + ~2 lines in `roles/digest.md` Waiting-on-you. Doc only; no code, no schedule. Decouples "what's blocked on a human" from "what unattended Workers can do next" -- which is the actual structural distinction that has been collapsed under "QUEUE.md".
  - Suggested priority: P1.

- [ ] [proposed-by: cowork-session / 2026-05-04] Authorize the night-auditor to commit purely-numeric fact updates to CLAUDE.md and STATE.md directly, without planner / Eric gate.
  - Why: stale-fact pattern is now load-bearing context: CLAUDE.md says "508 / 33 / 13 tests" (actuals 606 / 40 / 26 / 30); orphan-dirs claimed "entirely untracked" while DECISIONS-orphan-dirs.md established they ARE tracked; "5 commits ahead" propagated through 3 runs before correction. Auditor catches each one and files PROPOSED; PROPOSED never promotes; next auditor catches it again. The bottleneck is human gate on a class of edits that has zero policy / interpretation surface.
  - What it would do: amend `roles/night-auditor.md` to add a "Numeric facts auto-update" sub-step. SCOPE (allowed): test counts where the auditor just ran `bash scripts/test_all.sh` and parsed the suite-by-suite numbers; file byte counts where `git cat-file -s HEAD:<file>` is the source; age claims of the form "N days uncommitted" derived from `git log -1 --format=%cI`; orphan-dirs tracked counts derived from `git ls-files | wc -l`. NOT IN SCOPE: policy text, decisions, narrative, "Recent decisions" section in STATE.md, anything Eric wrote. Auditor commits with `chore(facts): refresh <fields> from <source>` prefix and logs the diff in the JOURNAL entry. Worker-style truncation guard from the P1 above applies. The "refresh CLAUDE.md test counts" PROPOSED can be retired (becomes a workflow not a one-off).
  - Risk / blast radius: small. Auditor gains a narrow write capability; offset by the explicit allow-list and the diff being in the journal. Zero source impact. The principle is "facts derivable from a command this run JUST executed are not a decision Eric needs to gate."
  - Suggested priority: P1.

- [ ] [proposed-by: cowork-session / 2026-05-04] Add a zero-risk auto-promote rule for PROPOSED items tagged `[auto-promote-after: Nh]`.
  - Why: 2 P1 zero-risk items (commit-or-stash and `git read-tree HEAD`) have sat unapproved for 6 days and 4 days respectively, both directly responsible for chronic morning friction (stale `.git/index`, plumbing-path commit workaround on every Worker run). PROPOSED has grown 11 -> 12 -> 13 in 5 days with 0 approvals. The `[x]`-only graduation rule is correct as a default but expensive when the item is reversible AND blast radius is documented as zero.
  - What it would do: planner role spec gains a step 3.5: "If a PROPOSED item carries `[auto-promote-after: Nh]` AND was filed >Nh ago AND is not marked `[needs-discussion]`, promote it to QUEUE.md with the suggested priority. Eric reverses by deleting the QUEUE entry; the auto-promote tag is set by whoever files (auditor / researcher / Worker), not by the planner." Whitelist categories Eric pre-approves (e.g. `chore(facts):`, `chore(git):`, `docs:`-only, doc-only changes to README without source touch). The two P1 zero-risk items today would have auto-promoted on day 2.
  - Risk / blast radius: workflow change; doc only. Reversible per item (delete the QUEUE entry). Adds discipline to "what counts as auto-promotable" -- whitelist criteria are a one-time decision Eric makes once, not per item.
  - Suggested priority: P2.

- [ ] [proposed-by: cowork-session / 2026-05-04] Add `TO-PUSH.md` + nightly digest one-line push checklist so manual-push step is the ONLY thing Eric does manually.
  - Why: Eric pushed `ae7d9fd` (worker-12pm bookkeeping) to origin/main manually becaus
- [ ] [proposed-by: night-auditor / 2026-05-05] Empirical refresh of role-file drift numbers: two of the three role files grew again overnight; the chain is a moving target, not a snapshot.
  - Why: prior PROPOSED entries (auditor 2026-05-03 P2 + auditor 2026-05-04 P2 corrective) treated the role-file drift as a fixed snapshot. Tonight's bash measurements (`wc -c` + `git cat-file -s HEAD:<file>` agreement on all three files): `roles/night-auditor.md` disk 3441b / HEAD 1830b (delta +1611b; +964b grown overnight from yesterday's 2477b); `roles/planner.md` disk 2682b / HEAD 2102b (delta +580b; unchanged); `roles/worker.md` disk 5588b / HEAD 2188b (delta +3400b; +1462b grown overnight from yesterday's 4126b). Combined drift now +5591b across three files. The 24h growth is consistent with worker-9am 2026-05-04 09:00's recorded finding that Eric batch-edited CLAUDE.md + roles/{night-auditor,worker}.md at 15:02:56 UTC adding "the new push policy + engineering-skill hooks." The 53-line on-disk version of `roles/night-auditor.md` (which carries the "Optional engineering skill hooks" + "GitHub-MCP audit" sections this auditor is following) is the post-batch state; HEAD's 36-line version pre-dates it.
  - What it would do: same three-options menu as the prior PROPOSED chain (a) restore-from-HEAD / (b) commit-with-intent / (c) re-finish-then-commit, applied to the post-Eric-batch-edit state. The commit-with-intent option (b) is the favored reading per worker-9am 2026-05-04 09:00's analysis ("Eric is actively iterating on these files; restoring-from-HEAD would discard the just-landed work"). If Eric picks (b), commit ALL THREE role files in one atomic chore commit titled e.g. `chore(roles): land 2026-05-04 batch edits + skill-hook integration` so the moving-target stops moving. CLAUDE.md (5272b -> 6127b on disk vs 4660b in HEAD; delta +1467b) is on the same edit-batch and should be bundled into the same commit -- the prior CLAUDE.md-waiver-commit PROPOSED (auditor 2026-05-01) is now structurally inseparable from this chain.
  - Risk / blast radius: low; same risk profile as the prior PROPOSED chain. Doc-only on (a)/(b)/(c). The audit-quality dimension repeats from the prior corrective: every audit until commit-with-intent fires will need to re-measure, and the prior PROPOSED chain's specific numbers will go stale within 24h of the next Eric edit.
  - Suggested priority: P2 (compounds with each Eric edit; supersedes the auditor 2026-05-04 P2 numerically but does not retire it -- the framing remains correct).

- [ ] [proposed-by: night-auditor / 2026-05-05] Normalize CRLF line endings on `samples/bpmn_validation/*` (`.bpmn`, `.puml`, `.skel.yaml`, `.xmi`).
  - Why: `git diff --stat HEAD --` on the four files has been showing 27/27 + 111/111 + 13/13 + 31/31 line replacements for ~8 days (mtime 2026-04-27 05:31 UTC). Per-byte comparison via Python `open(p,'rb').read()`: disk has CRLF (Windows checkout), HEAD has LF (Unix); content-equal after stripping CR on all four (`disk_size`/`head_size`/CRLF count: .xmi 2182/2155/27, .bpmn 6317/6206/111, .puml 427/414/13, .skel.yaml 804/773/31). This is a checkout-time `core.autocrlf` conversion that never got re-normalized. Each future `git diff` against these files surfaces noise; each future Worker run that touches the BPMN sample directory wades through phantom diffs. New finding: prior auditors enumerated three of the four files as ` M samples/bpmn_validation/{bpmn,puml,skel.yaml}` but consistently dropped `.xmi` from their listings -- a separate audit-quality concern (auditor enumerations should match `git status -s` output literally, not via human-style abbreviation).
  - What it would do: add a `.gitattributes` line at repo root, e.g. `samples/bpmn_validation/* text eol=lf` (or narrower `samples/bpmn_validation/*.{bpmn,puml,skel.yaml,xmi} text eol=lf`), then run `git add -u --renormalize` once. Net effect: working tree on Windows checks out with CRLF for editor-friendliness, but git stores LF. `git status` becomes clean for these files. Pairs with the BPMN P1 Camunda gate work -- the round-trip diff against Camunda's resave will be free of line-ending noise on the input fixtures. Optional broader scope: extend the rule to `*.bpmn` / `*.puml` everywhere (the orphan-dirs decision 3.7 may add more), but propose narrower to avoid touching anything else mid-Phase-2.
  - Risk / blast radius: low. Single-line `.gitattributes` change + one `git add -u --renormalize` invocation. Affects only the four sample fixtures (test-fixtures, not source code). No PyInstaller-bundle impact.
  - Suggested priority: P2.

- [ ] [proposed-by: night-auditor / 2026-05-05] Housekeeping: remove `ROADMAP.md.new` (stale untracked artifact) and either commit or revert the 7-day-uncommitted `schedule.json` comment-only update.
  - Why: (1) `ROADMAP.md.new` (15454 bytes, mtime 2026-05-04 17:58 UTC) is byte-identical to `ROADMAP.md` (verified: `diff ROADMAP.md ROADMAP.md.new` returns 0 lines). Leftover from the heredoc + tempfile + cp truncation-recovery pattern used during yesterday's bundle-v1-locked cowork-session; should have been removed at the end of that session. Currently a phantom untracked file that future `git status -s` runs continue to surface (sits in the `??` block alongside legitimate untracked items like `digests/` and the `research/2026-05-*-*.md` artifacts). (2) `schedule.json` working-tree edit (mtime 2026-04-28 23:08 UTC, ~7 days uncommitted): `git diff HEAD -- schedule.json` is purely a comment update -- the leading `_comment` field flips from "Do NOT register these tasks until Eva validation week clears (~2026-05-03). After that, ask Cowork to register..." to "Tasks registered with Cowork scheduled-tasks MCP on 2026-04-28 with IDs: process-tools-night-auditor, process-tools-researcher, process-tools-planner, process-tools-worker-{8am,9am,10am,11am,12pm}, process-tools-digest. To re-bootstrap, ask Cowork: 'Register the scheduled tasks defined in schedule.json.'" Doc-only diff; zero behavioral impact. Notably, every prior auditor + worker journal entry's "did NOT modify schedule.json" reassurance line (71 occurrences across JOURNAL.md) is technically true but elides that schedule.json was already dirty in their working trees -- the prior listings silently dropped this file from their dirty-files enumerations. That gap is a separate audit-quality concern (prior listings should match `git status -s` literally).
  - What it would do: `rm ROADMAP.md.new`; commit `schedule.json` as a single bookkeeping commit titled e.g. `chore(schedule): post-Eva-week comment update reflecting 2026-04-28 task registration`. Either Eric does this in evening review, or future Worker shifts silently keep ignoring both. Auditor cannot do either (READ-ONLY).
  - Risk / blast radius: zero on both. Single rm + single bookkeeping commit. No source impact, no schedule behavior change.
  - Suggested priority: P2.
