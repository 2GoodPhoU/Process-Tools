"""Convenience entry point — launches the GUI.

On Windows you can also rename this to ``run_gui.pyw`` so double-clicking
doesn't open a console window.
"""

from requirements_extractor.gui import main

if __name__ == "__main__":
    main()
