# Digest -- 2026-05-02

## TL;DR

Same shape as yesterday. All five scheduled Workers (8am-12pm) hit the no-eligible-items path -- 0 DONE for the second day in a row. Tests still 702/702. Throughput bottleneck is your `[x]`, not automation.

## Done today

- Nothing closed. Five Worker shifts produced JOURNAL appends only; no commits, no state-file churn after planner-7:10. 11th consecutive empty-queue Worker shift across the rolling chain.
- New artifact: `research/2026-05-02-night-auditor-confabulation-sweep.md` (researcher 04:00) -- 4-night audit-of-the-auditor sweep argues the existing audit-quality corrective should jump P2 -> P1.

## Waiting on you (3 open in NEEDS-INPUT)

1. **BPMN B1/B2/B3 pick** (worker-12pm 2026-04-30, ~48h pending) -- still the highest-leverage decision. **B1** = attended Worker session you kick off; **B2** = you run Option A (~15 min); **B3** = defer + accept structural validator + 40/40 unit tests as the gate.
2. **GitHub MCP unavailable** (auditor 2026-04-30) -- blocks PR/CI/push verification. Install + auth, or waive.
3. **Empty queue** (worker-11am 2026-04-30, planner-annotated) -- resolves automatically once any PROPOSED item lands.

Plus your CLAUDE.md BPMN-waiver edit is **uncommitted on disk for ~3 days** (auditor 2026-05-01 PROPOSED P2). `git show HEAD:CLAUDE.md > CLAUDE.md` repair would silently strip it.

## For your approval (12 open in PROPOSED, none approved)

Two zero-risk P1s remain the cleanest path to restoring throughput:

- **P1 -- Commit-or-stash the 0.6.1/0.6.2 patch line** (auditor 2026-04-29, ~5 days pending). 4 modified tracked + 12 untracked under `requirements-extractor/`.
- **P1 -- `git read-tree HEAD` to refresh `.git/index`** (auditor 2026-04-30, ~90h stale). Kills the chronic `MM` / `D ??` ghost-diffs and the daily plumbing-path commit workaround. Working-tree-safe.

10 P2s unchanged from yesterday: spec patch (`yaml` + `actor_heuristics`), CLAUDE.md test-count refresh, NUL-byte sweep in pre-commit, Read-vs-disk doc, README checklist fold-in, structural-diff helper, night-auditor-confabulation investigation, CLAUDE.md waiver commit. No high-risk items.

**Researcher recommends bumping the cowork-session 2026-04-30 audit-quality corrective from P2 -> P1** with 4 sub-amendments (content-parity-not-byte-parity, numeric self-consistency, full `git status -s` reporting, timezone discipline). Decision is yours.

## Queued for tomorrow

Same single item: BPMN P1 `[in-progress]`, attended-Worker-only. Tomorrow's 8am-12pm chain hits the identical empty-queue path unless tonight's review lands a PROPOSED `[x]` or your B1/B2/B3 pick.

## Notable / surprises

- **Audit-quality concern is now a documented pattern, not an outlier.** Researcher 04:00 flags 4-of-4 audit nights this week with at least one weak claim. Latest (auditor 00:05) confabulated "5 commits ahead of origin/main" inside an explicit anti-confabulation self-claim -- truth is `1`, verified by every Worker shift today via `git rev-list --count origin/main..HEAD`. Failure pattern degrades from gross invented text toward subtler quantitative/process errors.
- **Role-file content drift** (`roles/planner.md` + `roles/worker.md` byte-equal to HEAD but content-different by 13/42 lines) -- new finding from researcher 2026-05-02. Byte-count parity is no longer a sufficient HEAD-parity check; STATE.md "Known constraints" now flags this.
- **`.git/index.lock` is ~84h stuck**; `.git/index` ~85h stale. Every Worker commit still uses the plumbing-path workaround. The P1 `git read-tree HEAD` resolves both.
- Tests 702/702, unchanged since worker-9am 2026-04-30. No source touched in ~3 days.
