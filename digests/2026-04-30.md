# Digest -- 2026-04-30

## TL;DR

Productive morning: P0 foundational-files repair landed and 2 P2s closed. Then the queue ran dry by 11am. BPMN P1 unblocked in policy (your Option B) but still blocked in operations -- scheduled Workers can't service `request_access` dialogs.

## Done today

- **P0 -- Repair edit-tool truncation in 4 foundational guidance files** (CLAUDE.md, roles/{night-auditor,planner,worker}.md). worker-8am, 08:00. Restored via `git show HEAD:<file> > <file>` (binary redirect bypasses the Edit/Write cap). `git diff HEAD --` empty on all four. Tests 702/702. No commit (zero net delta).
- **P2 -- Edit-tool truncation sweep, 4 core requirements_extractor modules.** worker-9am, 09:00. cli/actors/models/extractor all byte-identical to HEAD. Read-only sweep; clean.
- **P2 -- PyInstaller spec audit.** worker-10am, 10:00. Findings appended to `requirements-extractor/DECISIONS.md` (~120 lines). Two real shipping-now gaps (`yaml`, `actor_heuristics`); 24 `_bundle` entries confirmed not dead weight; spec patch filed in PROPOSED, not edited.

## Waiting on you (4 open in NEEDS-INPUT, 1 answered)

1. **[answered]** BPMN GUI-gate -- you picked Option B + added the CLAUDE.md waiver. But see #4.
2. **GitHub MCP unavailable** -- blocks the night-auditor's PR/CI/push verification. Install + auth, or waive that section.
3. **Empty queue** (worker-11am) -- after the 3 closures, no eligible items remained. Today's 12pm Worker hit the same state.
4. **BPMN unattended-approval gap** (worker-12pm) -- Option B authorizes computer-use, but scheduled Workers can't click through `request_access` dialogs. Pick **B1** (kick off an attended Worker session yourself), **B2** (run Option A: open in Camunda Modeler + bpmn.io yourself, ~15 min), or **B3** (defer; accept structural validator + 40/40 unit tests as the gate, drop `[in-progress]`).

Plus your CLAUDE.md waiver edit is **uncommitted on disk** -- worth committing tonight so a future repair-from-HEAD doesn't silently undo it.

## For your approval (11 open in PROPOSED, none approved)

Two P1s most worth your attention tonight -- both compound across every future Worker run:

- **P1 -- Commit-or-stash the 0.6.1/0.6.2 patch line.** 4 modified tracked files + 12 untracked items in `requirements-extractor/` since 2026-04-27. Baselines carry an asterisk until this lands.
- **P1 -- `git read-tree HEAD` to refresh `.git/index`.** Kills the chronic `MM` / `D ??` ghost diffs forcing every Worker into the plumbing-path commit workaround. Working-tree-safe; touches only the index.

9 P2s including: spec patch (`yaml` + `actor_heuristics` to DocumentDataExtractor.spec), CLAUDE.md test-count refresh, NUL-byte sweep in pre-commit, doc the Read-vs-disk + Edit/Write-vs-disk disconnects in role files, `samples/bpmn_validation/README.md` checklist fold-in, structural-diff helper, and a new one from cowork-session investigating the night-auditor's confabulation of a non-existent push policy.

No high-risk items in the batch; everything is doc-only or scripts.

## Queued for tomorrow

`QUEUE.md` has 1 item: BPMN/Camunda P1 `[in-progress]`, gated on your B1/B2/B3 pick above. Without an answer or PROPOSED approvals, tomorrow's 8am-12pm chain will hit the same empty-queue state worker-11am and worker-12pm hit today.

## Notable / surprises

- **Write tool also truncates on overwrite** (planner 07:10 hit it on STATE.md). Previously documented as Edit-only. Established workaround: python binary-mode write from bash. The PROPOSED #3 (Read-vs-disk doc) should be expanded to cover Write.
- **Push-policy mystery resolved** (researcher 04:00, `research/2026-04-30-push-mystery.md`): Workers aren't pushing silently. You pushed manually at 17:55 yesterday during evening review. Three workflow-preference options spelled out; no correctness or security issue. The night-auditor's "policy and practice diverged" finding has been auto-resolved as `[x] no-action`.
- **Audit-quality concern** flagged in PROPOSED (cowork-session): the night-auditor's policy-violation finding cited an `automation/<role>-<YYYY-MM-DD>-<slot>` branch policy that doesn't exist anywhere in the repo. Same shape as the 2026-04-29 "NUL-byte sweep clean" false-clean. P2 to investigate where the phantom text came from.
- **`.git/index.lock` is now ~21h stuck** (mtime Apr 29 17:10). Every Worker commit today went via the plumbing-path workaround. Dies on its own once you approve the `git read-tree HEAD` PROPOSED.
- Tests 702/702 across all 4 tools, unchanged since worker-9am 09:00.
