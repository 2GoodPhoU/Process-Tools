# Digest -- 2026-05-05 (Tuesday)

## TL;DR

First real shipping day in a week: 5 DONE entries, 5 commits, 22+ empty-queue chain broken, 5-day zero-DONE chain broken. But the next-pull-ready set is exhausted -- tomorrow morning hits the same empty-queue path unless an Eric `[x]` lands tonight.

## Done today

- **3.1** `build.log` added to `requirements-extractor/.gitignore` -- worker-8am.
- **3.3 + 3.4** CLAUDE.md test counts refreshed (`~508/33/13` -> `606/40/30/26 = 702`) + harness wording (`pytest` -> `unittest discover`); bundled per 3.4 DoD -- worker-9am.
- **4.5** `RELEASE-NOTES-1.0.md` scaffold at repo root (175 lines, customer voice, cross-refs all four sub-tool CHANGELOGs) -- worker-10am.
- **5.4** `DECISIONS.md` 1.0 customer-handoff retrospective scaffold (4 required section headers, placeholders for 5.2 fill on handoff day) -- worker-11am.
- **2.2** Camunda import checklist folded into `samples/bpmn_validation/README.md` (11-row table + 6-step procedure inlined; research file retained as provenance) -- worker-12pm. Confirmed `samples/bpmn_validation/` is NOT under the orphan-dir off-limits rule (root-level path, not under any of the three named dirs).

## Waiting on you (3 open)

1. **BPMN P1 close-out** (worker-12pm 2026-04-30) -- pick B1 (attended Worker, ~15 min), B2 (you run Option A yourself, ~15 min), or B3 (defer + drop `[in-progress]`). ~120h since asked.
2. **GitHub MCP gap** (night-auditor 2026-04-30) -- install + auth, or waive. New push policy reduces urgency further.
3. **Empty-queue blocker** (worker-11am 2026-04-30) -- closed-then-reopened: today's chain consumed all five next-pull-ready items; tomorrow morning is empty again.

## For your approval (18 open, 0 approved)

The two zero-risk P1s remain the highest-leverage and still unmoved:

- **P1** Commit-or-stash 0.6.1/0.6.2 patch line (now 8 days uncommitted).
- **P1** `git read-tree HEAD` to refresh `.git/index` (~140h stale; chronic ghost-diff source).

New context for the role-file batch: researcher 2026-05-05 verified all three on-disk role files end on complete sentences with single trailing newlines -- they are clean intentional Eric edits, NOT truncations. **Recommendation: commit-with-intent on `roles/{night-auditor,planner,worker}.md` + CLAUDE.md as one bundled commit.** Auditor 2026-05-05 P2 (empirical refresh of the role-file-drift entry) is the structurally correct successor to the auditor 2026-05-03 entry, which carries two empirically-false claims.

Plus 3 new P2s from auditor 2026-05-05: role-file-drift refresh (above), CRLF normalization on `samples/bpmn_validation/*`, housekeeping (`ROADMAP.md.new` + `schedule.json` bundle).

## Queued for tomorrow

Phase 3 + Phase 4 + Phase 5 next-pull-ready set is **empty**. Without an `[x]`-approval tonight, tomorrow's 8am-12pm Worker chain will hit the empty-queue bailout path. BPMN P1 [in-progress] still sits at top -- attended-only; scheduled chain skips as before.

Highest-leverage approvals to refill the queue: (a) either zero-risk P1 above; (b) BPMN B1/B2/B3; (c) orphan-dir A/B/C pick (3.7); (d) role-file commit-with-intent; (e) cowork 2026-05-04 P1 post-edit-verification step (now load-bearing per today's two tail-drop incidents).

## Notable / surprises

- **Edit-tool size-cap-in-place tail-drop hit twice in 24h** (CLAUDE.md and QUEUE.md). Edit silently dropped `N` chars from EOF when adding `N` chars mid-file; size unchanged but tail truncated. Worker-9am detected via `wc -c` parity check; worker-11am and worker-12pm both pre-empted by switching to Python binary-mode rewrite + `cp` pattern. Post-edit-verification PROPOSED (3.5) is now empirically load-bearing.
- **Read-tool view masks PROPOSED.md truncation** (auditor 2026-05-05 00:05). Read showed 156 clean lines; bash/Python initially saw mid-word truncation. The Read-vs-disk skew is reliable for state files >~30 KB -- always cross-check via bash.
- **HEAD now == origin/main** (`f6fbb7e`, you pushed between 18:00 UTC 2026-05-04 and 00:05). Confirms the new push policy (local commit alone satisfies DoD) is working. Today's 5 commits are local-only and await your nightly push.
