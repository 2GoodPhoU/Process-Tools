# Digest -- 2026-05-06 (Wednesday)

## TL;DR

One DONE (3.8 ROADMAP refresh, worker-8am) followed by four empty-queue bailouts (9am/10am/11am/12pm). Your `261a674` bundle commit yesterday functionally retired ~5 long-pending PROPOSED items, but no PROPOSED `[x]`-approval landed today, so the queue stayed empty. Auditor + researcher converged on a sharper diagnosis of the `.git/index` problem -- it is structurally corrupt, not stale.

## Done today

- **3.8** `ROADMAP.md` refreshed post-`261a674`: requirements-extractor row version cell `0.6.2 (Unreleased on disk)` -> `0.6.2 (committed)`; last-updated stamp 2026-05-04 -> 2026-05-06; revision-history entry crediting your bundle commit -- worker-8am. Local commit `5c5b520` (atop `f734248`); plumbing-path workaround used.

## Waiting on you (3 open)

1. **BPMN P1 close-out** (worker-12pm 2026-04-30) -- pick B1 (attended Worker, ~15 min), B2 (you run it yourself, ~15 min), or B3 (defer + drop `[in-progress]`). ~167h since asked.
2. **GitHub MCP gap** (night-auditor 2026-04-30) -- install + auth, or waive. Low urgency; auditor self-corrected via reflog.
3. **Empty-queue blocker** (worker-11am 2026-04-30) -- recurring: today's 8am closed the only next-pull-ready item; 9am-12pm hit the bailout path.

## For your approval (19 open, 0 approved)

**Single highest-leverage `[x]` tonight: auditor 2026-05-06 P1 -- `.git/index` is structurally corrupt** (bad sha1 signature; three phantom `UU` entries with control-character paths; `git fsck` failing). Researcher 04:00 validated empirically -- live trailer SHA1 leaks raw stat bytes (`81a4`), object DB is intact, fresh index from `read-tree HEAD` reconstructs cleanly. One-command fix: `rm -f .git/index .git/index.lock && git read-tree HEAD`. **Approving this resolves both the new P1 and the existing 2026-04-30 P1** in one shot.

**Functionally retired by `261a674`** (awaiting your evening prune; planner did not silently delete per role spec): commit-or-stash 0.6.1/0.6.2 patch line (auditor 2026-04-29 P1); CLAUDE.md waiver commit (auditor 2026-05-01 P2); role-file drift chain (auditor 2026-05-03 + 2026-05-04 + 2026-05-05); `schedule.json` portion of housekeeping (auditor 2026-05-05).

**Other high-leverage approvals to refill tomorrow's queue**: BPMN B1/B2/B3 pick; orphan-dir A/B/C pick (3.7); cowork-session 2026-05-04 P1 post-edit verification (QUEUE 3.5; load-bearing for ~5 days now); auditor 2026-05-05 CRLF normalization on `samples/bpmn_validation/*`; auditor 2026-05-05 `ROADMAP.md.new` removal (still byte-equal cruft after worker-8am edited `ROADMAP.md` not `.new`).

## Queued for tomorrow

Next-pull-ready set is **empty again**. BPMN P1 [in-progress] still sits at top (attended-only; scheduled chain skips). Without an `[x]` tonight, tomorrow's 8am-12pm chain re-runs today's pattern: 8am may pick whatever planner can promote, 9am-12pm bail out.

## Notable / surprises

- **`.git/index` corruption empirically diagnosed.** Auditor 00:05 `git fsck` failed with `bad index file sha1 signature`; researcher 04:00 confirmed via SHA recompute over the live body and built a clean fresh index in `/tmp/`. Workers' `GIT_INDEX_FILE=/tmp/...` plumbing-path workaround has been shielding commits since 2026-04-30 (~7 calendar days) -- the chronic stale-index condition has crossed into structurally-broken territory. Approving the P1 is a one-command repair against the live `.git/index`; HEAD and object DB are intact.
- **Worker-9am `/tmp` staging recovery moment.** Heredoc to `/tmp/journal_append.md` returned `Permission denied`, then a stale prior-session payload at the same path got appended to JOURNAL.md. Detected immediately on tail inspection; truncated back to pre-append size (clean) and re-appended via inline Python heredoc. Lesson: probe `/tmp` writability or skip it entirely; not severe enough for a fresh PROPOSED.
- **Two planner-curation nits in `ROADMAP.md`** surfaced by worker-8am, left for the next planner pass: Phase-3 status cell for requirements-extractor row still reads `OPEN -- 0.6.1/0.6.2 uncommitted` post-`261a674`; test-totals citation date stuck on `night-auditor 2026-05-04 00:05` while the green baseline is `auditor 2026-05-06 00:05` (counts byte-identical at 606/40/30/26 = 702).
- **HEAD `4b114de`** (worker-8am follow-up, atop `5c5b520` = 3.8 closure). origin/main `f734248` (your last push 2026-05-05 14:51 UTC). Local 2 ahead / 0 behind; awaits your nightly push.
