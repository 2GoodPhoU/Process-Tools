# Digest -- 2026-05-03 (Sunday, last day of Eva validation week)

## TL;DR

Quiet day. Tests still 702/702 green; nothing shipped (3rd consecutive empty-DONE day); throughput remains bottlenecked on your approvals, not on automation. One new P2 PROPOSED filed overnight + a researcher trace of where the "5 commits ahead" mistake originated.

## Done today

- Nothing. DONE.md last entry is 2026-04-30. The 5-Worker chain (8am-12pm) all hit the empty-queue path -- the only QUEUE item is the BPMN P1 marked attended-only and they correctly skipped.

## Waiting on you (3 open)

1. **BPMN P1 close-out** (worker-12pm 2026-04-30) -- pick B1 (attended ad-hoc Worker, ~10-15 min of your time), B2 (you run Option A yourself in Camunda, ~15 min), or B3 (defer + drop `[in-progress]`, weakest gate). ~67h since asked.
2. **GitHub MCP gap** (night-auditor 2026-04-30) -- install + auth a GitHub MCP, or waive the PR/CI/push-verification audit section. No urgency; auditor self-corrected via `git reflog`.
3. **Empty-queue blocker** (worker-11am 2026-04-30) -- still rolled forward; resolved by approving any PROPOSED below.

## For your approval (13 open, 0 approved)

Two zero-risk P1s alone would unblock the morning chain:

- **P1** Commit-or-stash the 0.6.1/0.6.2 patch line (~840 LOC tracked + ~1933 LOC untracked, now 6 days uncommitted).
- **P1** `git read-tree HEAD` to refresh `.git/index` (92h stale; cause of the chronic `MM` / `D ??` ghost-diffs every Worker hits).

Other 11 are P2: pre-commit hook fix (Windows Python discovery), `test_all.sh` fail-loud on load errors, fold Camunda checklist into samples README, BPMN structural-diff helper (orphan-dirs-blocked), NUL-byte sweep, refresh CLAUDE.md test counts, document Read-vs-disk gap as project rule, PyInstaller spec patch (yaml + actor_heuristics), confabulation investigation, commit the BPMN waiver block to CLAUDE.md (~67h uncommitted), restore-or-commit role-file content drift (NEW today, 3 concrete options).

## Queued for tomorrow

- **[in-progress] [P1]** Validate BPMN 2.0 emitter against Camunda Modeler -- still attended-only; scheduled chain will skip again.

That is the entire queue.

## Notable / surprises

- **Researcher traced the "5 commits ahead" error to its origin**: `research/2026-05-03-five-commits-ahead-propagation.md` shows the wrong number originated at worker-11am 2026-05-01 11:00 (not at auditor-4 as previously assumed). The corrective IS working downstream -- 8 consecutive runs since 2026-05-02 04:00 grounded the right number (1 ahead, verified). The audit layer remains the single open gap.
- **Role-file content drift surfaced**: `roles/planner.md` (13 lines) and `roles/worker.md` (42 lines) are byte-equal to HEAD but content-different. Every Planner/Worker run since 2026-04-30 14:07 UTC has been operating against the on-disk version, not HEAD. Auditor filed P2 with three options.
- **Cleanest audit night this week** -- 0 new code-level findings; only the role-file-drift bookkeeping P2.
- Eva validation week ends today; no scheduled-task changes per CLAUDE.md.
