# Digest — 2026-04-29

## TL;DR
Productive day. The P0 working-tree blocker landed first thing, then the BPMN P1 closed structurally (24/24) but stalled at the GUI gate, then two follow-up P1/P2 items shipped. Three DONE entries; one decision waiting on you.

## Done today
- **[P0] models.py truncation repaired** (worker-8am). Removed the 7-line corrupted block AND stripped 192 trailing NUL bytes the night-auditor's sweep missed. Test discovery jumped 109 → 606. Suite green.
- **[P1] requirements-extractor baseline captured** (worker-11am). `BASELINE-2026-04-29.md`: 606/0/0/0, zero flakies, ~6.95 s wall-clock. Documented as the offline-network configuration (no spaCy, rule-based fallback covers everything) — which is what the air-gapped target ships.
- **[P2] heuristic-coverage audit** (worker-12pm). New `requirements-extractor/DECISIONS.md`. All 10 rules have ≥1 positive test; 5 lack negative tests; 3 carry mostly-unexercised alternation lists. Dead `elif` in `_h_appositive` flagged.

## Waiting on you (1)
- **BPMN/Camunda GUI gate** (from worker-9am). Programmatic structural validation passed 24/24, unittest pins 40/40 — but the Camunda Modeler 5.x desktop walk + bpmn.io drag-drop + save-and-diff steps need a human or an explicitly-authorized computer-use Worker. Pick: **(A)** ~15 min manual walk yourself, **(B)** waiver in `roles/worker.md` to let a Worker drive Camunda Modeler / Chrome MCP, **(C)** defer.

## For your approval (7 in PROPOSED.md)
Two are blocking-adjacent and worth deciding tonight:
- **[P1] Commit-or-stash the 0.6.1/0.6.2 patch line** (night-auditor). 13 modified files + 12 untracked items (~2.7K LOC) carried in the working tree since 2026-04-27. Today's baseline carries an asterisk until this lands.
- **[P2] Fix the Windows pre-commit hook Python discovery** (night-auditor). Hook is the documented safety net for edit-tool truncation; bypassed via `--no-verify` on 2026-04-27. Today's P0 is exactly what the hook would have caught.

The rest are P2 and can age:
- Broaden NUL-byte sweep to all tracked text files (worker-8am — today's evidence is strong).
- Make `scripts/test_all.sh` fail-loud on module-load errors (night-auditor).
- Refresh CLAUDE.md test counts (night-auditor — partially superseded; counts updated in the file already).
- Fold Camunda checklist into `samples/bpmn_validation/README.md` (researcher).
- Stdlib BPMN structural-diff helper (researcher — gated on orphan-dirs decision; the 9am Worker already wrote the before-half as a scratch script).

## Queued for tomorrow
1. **[P1, in-progress]** BPMN/Camunda — skipped by Workers until your NEEDS-INPUT answer lands.
2. **[P2]** Edit-tool truncation sweep across all four core `requirements_extractor/` modules.
3. **[P2]** PyInstaller spec audit vs. current imports.

## Notable / surprises
- **The NUL-byte hazard generalizes.** It hit `models.py` overnight and `QUEUE.md` during the 10am Worker's run. Read tool silently filters NULs; Edit can introduce them. Workers have started defaulting to Python binary-mode writes for state files.
- **Three Workers in a row hit stuck `.git/*.lock` files** (HEAD.lock then index.lock — Windows-side ownership) and had to use `GIT_INDEX_FILE=/tmp/...` + direct ref-write plumbing to commit. If this recurs tomorrow it deserves its own PROPOSED item plus a one-liner in `roles/worker.md`.
- **Edit tool silently failed to persist JOURNAL.md changes** during the 10am run (Read sees staged version; bash/Python see disk version). Index-vs-HEAD-vs-working-tree state on the state files is divergent — JOURNAL.md is partially truncated mid-9am-entry on disk. Reconciliation deferred to tonight.
- **Premise correction landed in DECISIONS-orphan-dirs.md**: the three sibling dirs are NOT untracked (CLAUDE.md/STATE.md/COMMIT_PLAN.md still describe them that way). They were committed during the 2026-04-25/26 workshop. 19 + 20 + 8 files. Decision still owed.
