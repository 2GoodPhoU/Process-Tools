# Done — Process-Tools

> Completed items. Workers move things here when finished. Lets future runs see what's been addressed so they don't re-propose it.

## Format

```
## YYYY-MM-DD
- Title — done by role at HH:MM. Outcome: ...
  - Commit: <sha or "no commit">
  - Tests: pass | fail | n/a
```

---

## 2026-04-29

- [P0] Repair edit-tool truncation in `requirements-extractor/requirements_extractor/models.py` — done by process-tools-worker-8am at 08:00. Outcome: removed the duplicated comment block + corrupted residue (`tructural events."""` on line 205 plus stray `requirement: "Requirement"` field) AND stripped 192 trailing NUL bytes that were silently sitting at EOF (Read tool filters NULs, so the night-auditor's NUL sweep missed them — flagged below). File now py_compiles clean. The legitimate `parent_id` field on line 148 (0.6.2 multi-action work) is preserved untouched.
  - Commit: `fix: repair models.py edit-tool truncation + strip trailing NUL bytes` (latest commit on `main`, recorded by subject not SHA so an amend can't desync this entry). Pre-commit hook ran without `--no-verify` this time (which was unexpected given the auditor's hook-bypass note from this morning — worth noting for the evening review). 1 commit ahead of `origin/main`; NOT pushed per `roles/worker.md` and `CLAUDE.md`.
  - Tests: pass — `bash scripts/test_all.sh` reports ALL GREEN, 702 tests across 4 tools (process-tools-common 26, compliance-matrix 30, nimbus-skeleton 40, requirements-extractor 606). Prior 27 ERRORs + 5 FAILs cleared. Note: requirements-extractor's `Ran N tests` jumped 109 → 606 because the syntax error was preventing module load, which made `unittest discover` undercount.


- [P1] Baseline pytest run for `requirements-extractor/` — done by process-tools-worker-11am at 11:00. Outcome: produced `requirements-extractor/BASELINE-2026-04-29.md` (renamed from -28 per planner gate; marker dated the day the suite actually went green). Suite: 606 / 0 / 0 / 0 (pass / fail / error / skip), zero flakies across two consecutive verbose runs (test-id projection diff was empty), wall-clock 6.855–7.045 s across four runs (mean ~6.95 s; variance under 200 ms). Environment captured: Python 3.10.12, openpyxl 3.1.5, PyYAML 6.0.3, python-docx 1.2.0, pdfplumber 0.11.9. **spaCy and en_core_web_sm are NOT installed** — this is the offline-network configuration; the rule-based heuristic fallback covers the entire failure surface. The 0.6.1/0.6.2 patch line is uncommitted in the working tree (4 modified tracked files, +639/−21 LOC; 16 untracked items in `requirements-extractor/`); per the QUEUE definition-of-done, the baseline is captured as "tree as it stands today" and the doc records both the HEAD SHA (`96aab20`) and the working-tree delta. Wording note: queue title and commit message read "pytest" but the harness is `unittest discover` — flagged separately in PROPOSED.md (night-auditor 2026-04-29).
  - Commit: `baseline: requirements-extractor pytest snapshot 2026-04-29` — see commit log; not pushed per `roles/worker.md` and `CLAUDE.md`. Used the same `.git/index.lock` plumbing-path workaround the 9am/10am Workers had to use (Windows file-lock blocks `git`'s own unlink, plus the previously-noted `.git/HEAD.lock` — see flagged-for-evening-review).
  - Tests: pass — `bash scripts/test_all.sh` ALL GREEN, 702 tests across 4 too


- [P2] Audit regression coverage for the 10 actor-extraction heuristics — done by process-tools-worker-12pm at 12:00. Outcome: created `requirements-extractor/DECISIONS.md` (file did not exist; opened with the audit as its first entry). Per-heuristic table maps each rule to its owning function (`_h_by_agent` … `_h_appositive` in `actor_heuristics.py`) and its named tests in `tests/test_actor_heuristics.py`. Verdict: all 10 heuristics have at least one positive test (no rule is uncovered); 5 of 10 are fully covered (rules 1, 2, 4, 5, 6 have negative tests); 5 of 10 are partially covered (rules 3, 7, 8, 9, 10 — positives only, no false-positive negative). Three rules (5, 7, 8) carry alternation lists where most branches are unexercised: rule 5 pins 2 of 6 conditional triggers, rule 7 pins 1 of 9 passive verbs and 1 of 5 modal forms, rule 8 pins 2 of 7 hyphen suffixes. Out-of-scope code finding logged in DECISIONS.md, not actioned: `_h_appositive` has a redundant unreachable `elif` branch. Recommendations section spells out what a future remediation would do (~5 negative fixtures + ~14 alternation fixtures) but no new tests written — DoD said audit, not remediate.
  - Commit: `worker-12pm: heuristic regression-coverage audit (DECISIONS.md)` — see commit log; not pushed per `roles/worker.md` and `CLAUDE.md`.
  - Tests: pass — `python3 -m unittest tests.test_actor_heuristics` reports 36 / 36 ok in 0.001 s. `bash scripts/test_all.sh` reports ALL GREEN, 702 tests across 4 tools — no regression vs. the 11am Worker's run (audit is doc-only; no code touched).


## 2026-04-30

- [P0] Repair edit-tool truncation in 4 foundational guidance files (CLAUDE.md, roles/night-auditor.md, roles/planner.md, roles/worker.md) -- done by process-tools-worker-8am at 08:00. Outcome: restored all four files via `git show HEAD:<file> > <file>` (binary-mode redirect, bypasses the documented Edit/Write truncation cap that Edit, Write, AND yesterday's planner Write call all hit). Post-repair `wc -lc` matches HEAD byte-for-byte: CLAUDE.md 82/4660, roles/night-auditor.md 36/1830, roles/planner.md 37/2102, roles/worker.md 35/2188. Tail-80 of each ends on a complete sentence + newline (CLAUDE: "...Phase 1.", night-auditor: "...without them", planner: "...evening review.", worker: "...renamed)."). Zero NUL bytes across all four. `git diff --stat HEAD --` shows zero delta on every file. Workers reading via Read no longer disagree with workers reading via bash/Python -- the disconnect is closed for these four files until the next Edit/Write call hits the cap again.
  - Commit: no commit (the run produced ZERO net code changes -- the disk content was restored to exactly match HEAD, so `git diff HEAD --` is empty on all four files; per `roles/worker.md` step 6, the commit-and-push flow only applies "if the run produced code changes"). The chronic stuck `.git/index.lock` (Apr 29 17:10, 14h+) was noted but did not need to be bypassed -- there's nothing to commit.
  - Tests: pass -- `bash scripts/test_all.sh` ALL GREEN, 702 tests across 4 tools (process-tools-common 26 / compliance-matrix 30 / nimbus-skeleton 40 / requirements-extractor 606). Same as the night-auditor's 00:05 run, no regression. Sanity check only -- the P0 was a doc-content repair, not a code change.

- [P2] Edit-tool truncation sweep (broadened to all four core requirements_extractor modules) -- done by process-tools-worker-9am at 09:00. Outcome: read-only sweep; all four files clean. `cli.py` 631/23726, `actors.py` 365/14000, `models.py` 204/8436, `extractor.py` 288/11316 -- byte-identical to HEAD on `wc -lc` AND `tail -15` AND `git diff --stat HEAD --` (empty). Zero NUL bytes anywhere; all four end on a complete sentence + newline. Tail markers: cli ends "sys.exit(main())", actors ends "return None", models ends "...keep the imports simple for callers.", extractor ends ")". The hazard CLAUDE.md flagged for cli/actors and the night-auditor caught on models is NOT present in the working tree as of HEAD `382ee397`.
  - Commit: no commit (read-only sweep; the run produced ZERO net changes -- nothing to commit beyond state-file bookkeeping). State-file updates (QUEUE.md / DONE.md / JOURNAL.md) committed separately as the worker bookkeeping pattern; see commit log.
  - Tests: n/a -- DoD specifies `git diff` and `tail -15` checks only, no test run required for a read-only sweep. Last green test baseline is the night-auditor's 00:05 run (702/702 across 4 tools), unchanged since.

- [P2] Audit the PyInstaller spec against current imports -- done by process-tools-worker-10am at 10:00. Outcome: stdlib-only `ast`-based walk of all 25 `.py` files under `requirements_extractor/` plus `run_gui.py`. Compared third-party imports against the spec's `_bundle()` calls (29 entries) and the explicit `requirements_extractor.*` hiddenimports list (24 entries). Findings appended as a new section in `requirements-extractor/DECISIONS.md` (2026-04-30 entry, ~120 lines). **Two shipping-now gaps**: (1) `yaml` (PyYAML, used in `config.py:391` + `keywords_loader.py:100`) is missing from `_bundle` -- offline-network YAML config / YAML keyword files fail with the project's own "Install with: pip install pyyaml" message; (2) `requirements_extractor.actor_heuristics` (the offline rule-based fallback CLAUDE.md calls "load-bearing") is missing from explicit hiddenimports despite being reached only via lazy `from .actor_heuristics import` inside `actors.py:252` -- the dynamic-dispatch pattern the spec author specifically flagged. **Two pre-emptive gaps**: `requirements_extractor.compound` and `requirements_extractor.multi_action` are imported at top-level in `parser.py:271/275` but are still untracked working-tree files (0.6.1/0.6.2 patch line); will need spec lines added when that patch line is committed. **Twenty-four `_bundle` entries that look like dead weight are not** -- they are spaCy + pdfplumber transitive deps whose `catalogue`/registry-based loading defeats PyInstaller's static analysis (the spec's own comments at lines 41-43, 73-75 spell out the policy). **Excludes list is healthy** -- 13 entries, all confirmed absent from shipped-binary code paths. PROPOSED.md item filed (worker-10am / 2026-04-30, P2) capturing the two-line spec patch for evening-review approval; spec NOT edited per DoD ("Do NOT edit the spec -- propose changes via PROPOSED.md if any are warranted").
  - Commit: `worker-10am: P2 PyInstaller spec audit (DECISIONS.md + PROPOSED entry)` -- bookkeeping commit on `requirements-extractor/DECISIONS.md`, `PROPOSED.md`, `QUEUE.md`, `DONE.md`, `JOURNAL.md` only. No source code in this commit (none was touched). Used the `.git/index.lock` plumbing-path workaround the 9am-12pm Workers used yesterday + this morning's 9am Worker (`GIT_INDEX_FILE=/tmp/...` + `git read-tree HEAD` to start from a clean tree per worker-9am's lesson, then `git add` the 5 bookkeeping files, then `git write-tree` + `git commit-tree` + direct ref-write). NOT pushed per `roles/worker.md` and `CLAUDE.md`.
  - Tests: n/a -- DoD is doc-only (audit findings appended to DECISIONS.md, no spec edit, no source edit). Last green baseline is the night-auditor's 00:05 run + worker-8am's 08:00 sanity check + worker-9am's truncation sweep (read-only): 702/702 across 4 tools, unchanged.


## 2026-05-05

- [P2] Add `build.log` to `requirements-extractor/.gitignore` (QUEUE next-pull-ready 3.1) -- done by process-tools-worker-8am at 08:00. Outcome: one-line edit under the PyInstaller block (line 14, between `dist/` and `*.manifest`); `git check-ignore -v requirements-extractor/build.log` now prints `requirements-extractor/.gitignore:14:build.log` (rule fires); `git status --short | grep -E 'build\.log'` returns empty (file no longer dirties the working tree). build.log on disk is 128 KB / mtime 2026-04-27 (close to the queue note's 131 KB). No code touched; pytest unaffected by definition. Closes the long-standing PROPOSED chain on this artifact (no PROPOSED entry was filed for 3.1 specifically, but the next-pull-ready set in QUEUE was the structural successor). First DONE entry in 6 calendar days; ends the empty-DONE chain.
  - Commit: `worker-8am: P2 add build.log to requirements-extractor/.gitignore (QUEUE 3.1)` -- bookkeeping commit on `requirements-extractor/.gitignore`, `QUEUE.md`, `DONE.md`, `JOURNAL.md`. Used the `.git/index.lock` plumbing-path workaround per `roles/worker.md` step 5 + the worker-10am 2026-04-30 procedure (GIT_INDEX_FILE=/tmp/... + `git read-tree HEAD` + `git add` + `git write-tree` + `git commit-tree` + direct ref-write). Local commit only; remote push deferred per CLAUDE.md push policy (Eric pushes nightly).
  - Tests: n/a -- DoD is doc/config only (`.gitignore` rule add); no `.py` touched; the existing 702/702 green baseline from auditor 2026-05-05 00:05 stands unchanged.

- [P2] Refresh CLAUDE.md test-count + harness wording (QUEUE next-pull-ready 3.3 + 3.4, bundled per 3.4 DoD) -- done by process-tools-worker-9am at 09:00. Outcome: one-line edit in CLAUDE.md "Stack & conventions". Old: `pytest (~508 tests in requirements-extractor + 33 in nimbus-skeleton + 13 in process-tools-common)`. New: `unittest discover (606 in requirements-extractor + 40 in nimbus-skeleton + 30 in compliance-matrix + 26 in process-tools-common = 702 total)`. Numbers sourced from a fresh `bash scripts/test_all.sh` this shift (ALL GREEN, 702 tests across 4 tools; counts byte-identical to the auditor 2026-05-05 00:05 run). Closes the long-standing PROPOSED night-auditor 2026-04-29 P2. Edit was bundled because 3.4's DoD explicitly says "Bundle into 3.3's commit" -- not scope creep.
  - Commit: `worker-9am: P2 refresh CLAUDE.md test counts + harness wording (QUEUE 3.3+3.4)` -- bookkeeping commit on `CLAUDE.md`, `QUEUE.md`, `DONE.md`, `JOURNAL.md`. Used the `.git/index.lock` plumbing-path workaround per `roles/worker.md` step 5 + the worker-10am 2026-04-30 procedure. Local commit only; remote push deferred per CLAUDE.md push policy (Eric pushes nightly).
  - Tests: pass -- `bash scripts/test_all.sh` ALL GREEN, 702 tests across 4 tools. No regression vs. auditor 2026-05-05 00:05. Note: change is doc-only (no `.py` touched), but DoD test run is documented anyway since the new test-count claim depends on this run's output.
  - Hazard surfaced: the Edit tool hit the **size-cap-in-place tail-drop hazard** documented in worker-8am 2026-05-05 08:15 follow-up. Edit added 42 chars mid-file (the harness-wording delta) and silently dropped 42 chars from the tail (the last line of CLAUDE.md, ` hook beyond Sabrina-Local-AI in Phase 1.\n`). Detected by post-edit `wc -c` showing the file size was unchanged at 6127 bytes despite a +42-char content delta. Fixed via Python binary-mode rewrite that restored the dropped tail; final size 6169 bytes / 87 lines / 0 NUL / clean trailing newline. **2nd documented occurrence of this specific failure mode in 24h** -- the post-edit-verification step PROPOSED (cowork-session 2026-05-04 P1) is now load-bearing for any Edit call against CLAUDE.md or QUEUE.md.

- [P2] Draft `RELEASE-NOTES-1.0.md` scaffold at repo root (QUEUE 4.5, next-pull-ready) -- done by process-tools-worker-10am at 10:00. Outcome: created `RELEASE-NOTES-1.0.md` (175 lines, 5839 bytes, 0 NUL bytes, clean trailing newline). Customer-facing tone per CLAUDE.md operator voice; grep for automation jargon (worker / queue / automation / planner / night-auditor / todo / in-progress / proposed / done / next-pull-ready / gating / backlog) returns 0 hits. One H2 section per sub-tool: requirements-extractor 1.0.0, nimbus-skeleton 1.0.0, compliance-matrix 1.0.0, process-tools-common 1.0.0, plus metadata sections (What this release is, How to read these notes, Upgrade and installation, Known issues, Cross-references, Revision history). Each sub-tool section explicitly cross-references its `<sub-tool>/CHANGELOG.md`, with all four CHANGELOG references restated in the bottom Cross-references section. Section bodies are intentionally unpopulated -- the queue entry says "scaffold ahead of Phase 4 so the entry is ready to fill on tag day"; content fill happens during 4.1-4.4 / 4.6 cuts. Closes the long-standing next-pull-ready 4.5 entry; second-of-two remaining next-pull-ready items closed today (8am closed 3.1, 9am closed 3.3+3.4 bundled, 10am closes 4.5; 5.4 still pending).
  - Commit: `worker-10am: P2 RELEASE-NOTES-1.0.md scaffold at repo root (QUEUE 4.5)` -- bookkeeping commit on `RELEASE-NOTES-1.0.md` (new), `QUEUE.md`, `DONE.md`, `JOURNAL.md`. Used the `.git/index.lock` plumbing-path workaround per `roles/worker.md` step 5 + the worker-10am 2026-04-30 procedure (GIT_INDEX_FILE=/tmp/... + `git read-tree HEAD` + `git add` + `git write-tree` + `git commit-tree` + direct ref-write). Local commit only; remote push deferred per CLAUDE.md push policy (Eric pushes nightly).
  - Tests: n/a -- DoD is doc-only (scaffold creation, no `.py` touched). Last green baseline is auditor 2026-05-05 00:05 (702/702 across 4 tools); unchanged.

- [P2] Draft `DECISIONS.md` CUSTOMER_HANDOFF retrospective entry (QUEUE 5.4, next-pull-ready) -- done by process-tools-worker-11am at 11:00. Outcome: appended a single dated entry to root `DECISIONS.md` (2026-05-05 -- 1.0 customer-handoff retrospective (scaffold)). Decision-doc voice per CLAUDE.md and the file's preamble (operator voice; state the call, the reason, what would change the call); structure mirrors the BPMN DI entry already on file (Decision / Why now / What this entry will record / Cross-references / What would change the call / Files). Four required section headers committed as scaffold (`### What shipped`, `### What slipped`, `### What surprised`, `### What to do differently for 1.1`), each with a single-line `_Filled in on handoff day per QUEUE 5.2._` placeholder. Cross-references restated explicitly to `RELEASE-NOTES-1.0.md` and the four sub-tool CHANGELOGs (`requirements-extractor`, `nimbus-skeleton`, `compliance-matrix`, `process-tools-common`), plus `ROADMAP.md` (1.1 / Post-1.0 section seeded by QUEUE 5.3) and the `1.0.0` bundle tag (per QUEUE 4.7). DECISIONS.md grew 4169 -> 7151 bytes / 87 -> 164 lines / 0 NUL bytes / clean trailing newline (`_QUEUE 5.2._\n`). Closes the last next-pull-ready item in today's chain (8am closed 3.1, 9am closed 3.3+3.4 bundled, 10am closed 4.5, 11am closes 5.4). Phase 3 + Phase 4 + Phase 5 next-pull-ready set is now empty; tomorrow's morning chain will hit the empty-queue bailout unless Eric `[x]`-approves at least one PROPOSED item during evening review (the two zero-risk P1s -- commit-or-stash 0.6.1/0.6.2 + `git read-tree HEAD` to refresh `.git/index` -- remain the highest-leverage approvals).
  - Commit: `worker-11am: P2 DECISIONS.md CUSTOMER_HANDOFF retrospective scaffold (QUEUE 5.4)` -- bookkeeping commit on `DECISIONS.md`, `QUEUE.md`, `DONE.md`, `JOURNAL.md`. Used the `.git/index.lock` plumbing-path workaround per `roles/worker.md` step 5 + the worker-10am 2026-04-30 procedure (GIT_INDEX_FILE=/tmp/... + `git read-tree HEAD` + `git add` + `git write-tree` + `git commit-tree` + direct ref-write). Local commit only; remote push deferred per CLAUDE.md push policy (Eric pushes nightly).
  - Tests: n/a -- DoD is doc-only (single dated DECISIONS.md entry, no `.py` touched, no spec touched, no source touched). Last green baseline is auditor 2026-05-05 00:05 (702/702 across 4 tools); unchanged. The two prior worker shifts this morning that DID change CLAUDE.md (9am bundled 3.3+3.4) and added a root file (10am scaffolded RELEASE-NOTES-1.0.md) also flagged no test impact -- this run is the third doc-only DONE in a row today.
  - Hazard pre-empted: Edit-tool size-cap-in-place tail-drop hazard (worker-9am 2026-05-05 09:00 follow-up; 2nd documented occurrence in 24h). Did NOT use Edit on DECISIONS.md or QUEUE.md -- both edits done via Python binary-mode read+write through bash. Pre-edit DECISIONS.md was 4169 bytes; post-edit 7151 bytes (delta 2982, exactly matches `/tmp/decisions_append.md` size; no silent drop). QUEUE.md trim from 16522 -> 15986 bytes (delta -536, exactly the 5.4 block size; tail end is the last char of 5.3's "Skill: `engineering:code-review`.\\n" with no orphaned separator). Post-edit `python3 -c "data.count(b'\\\\x00')"` reports 0 NUL bytes on both files. The post-edit-verification step PROPOSED (cowork-session 2026-05-04 P1, QUEUE 3.5) is now load-bearing for any state-file edit; this shift's evidence supports the case for `[x]`-approving it during evening review.

- [P2] Fold the Camunda import checklist into `samples/bpmn_validation/README.md` (QUEUE 2.2, [phase-2] [doc-only]) -- done by process-tools-worker-12pm at 12:00. Outcome: rewrote the README's "How to validate" section to inline the section-2 pass-criterion table (11 rows: Pool / Lanes / Tasks / Start event / End event / Sequence flows / Cross-lane edges / Gateways / Text annotations / `BPMNShape` for every flow node / `BPMNEdge` for every sequence flow with >=2 waypoints) plus the section-6 procedure (6 numbered steps: open in Camunda Modeler 5.x desktop -> walk canvas vs. table -> repeat in demo.bpmn.io -> save in Camunda + structural-diff -> append DECISIONS.md entry on pass / write to NEEDS-INPUT.md on fail). `research/2026-04-29-camunda-import-checklist.md` retained as provenance and explicitly cross-referenced from the new "How to validate" section header. Lint-warning paragraph (Camunda 7 / Zeebe runtime concerns are NOT BPMN-2 conformance failures) preserved from the research file's section 4. README grew 56 -> 94 lines / 2577 -> 5032 bytes; well within the DoD's "<= 2 pages" envelope (visual page = ~50-60 lines; the file renders to ~1.5-2 pages depending on font / viewport). 0 NUL bytes; ASCII text per `file(1)`; clean trailing newline.
  - Path note (DoD dep resolution): the queue-section intro lumped 2.2 in with "2.1/2.2/2.4 also gated on the orphan-dir tracked-vs-ignored decision (3.7) because they touch `nimbus-skeleton/`," but 2.2's per-item DoD line explicitly notes "(path is at root, not under `nimbus-skeleton/`)" and asks the worker to "confirm on pull whether `samples/bpmn_validation/` falls under the orphan-dir off-limits rule." CLAUDE.md off-limits rule names three dirs by literal path: `compliance-matrix/`, `nimbus-skeleton/`, `process-tools-common/`. `samples/bpmn_validation/` is at repo root, not under any of those three. Confirmed: NOT off-limits; 2.2 was structurally pickable. The section-intro mis-bucket is a planner curation nit (worth a one-line correction in the next planner pass; not a blocker).
  - Commit: `worker-12pm: P2 fold Camunda import checklist into samples/bpmn_validation/README.md (QUEUE 2.2)` -- bookkeeping commit on `samples/bpmn_validation/README.md`, `QUEUE.md`, `DONE.md`, `JOURNAL.md`. Used the `.git/index.lock` plumbing-path workaround per `roles/worker.md` step 5 + the worker-10am 2026-04-30 procedure (GIT_INDEX_FILE=/tmp/... + `git read-tree HEAD` + `git add` + `git write-tree` + `git commit-tree` + direct ref-write). Local commit only; remote push deferred per CLAUDE.md push policy (Eric pushes nightly).
  - Tests: n/a -- DoD is doc-only (samples README; no `.py` touched, no spec touched, no source touched). Last green baseline is auditor 2026-05-05 00:05 (702/702 across 4 tools); unchanged. Fourth doc-only DONE in a row today (8am gitignore, 9am CLAUDE.md, 10am RELEASE-NOTES-1.0.md, 11am DECISIONS.md, 12pm samples README).
  - Hazard pre-empted: Edit-tool size-cap-in-place tail-drop hazard (worker-8am 8:15 + worker-9am 9:00). Did NOT use Edit/Write on the README -- wrote the new content via heredoc to `/tmp/new_readme.md` and `cp` over the target. Post-write verification: bytes=5032 / NULs=0 / lines=94 / ends_newline=True / `file(1)`=ASCII text / all 11 table rows + 6 procedure steps + provenance ref grep-confirmed.


## 2026-05-06

- [P2] Refresh `ROADMAP.md` sub-tool status table now that 0.6.1 + 0.6.2 patch lines have landed in `261a674` (QUEUE 3.8, [phase-3] [doc-only], next-pull-ready) -- done by process-tools-worker-8am at 08:00. Outcome: three changes to `ROADMAP.md` (15454 -> 15929 bytes / 342 -> 343 lines / 0 NUL / clean trailing newline). (1) `**Last updated:** 2026-05-04.` -> `**Last updated:** 2026-05-06.` (line 7). (2) Sub-tool status table requirements-extractor row version cell `0.6.2 (Unreleased on disk)` -> `0.6.2 (committed)` (line 218). (3) Revision-history append (new line 343) crediting Eric's `261a674` bundle commit (2026-05-05) for landing the 0.6.1 + 0.6.2 patch lines (compound.py + multi_action.py + tests + fixtures + baselines + parser + config + CHANGELOG + PATCH notes + .gitignore) in HEAD. Test totals unchanged at 702 (606 / 40 / 30 / 26) verified this shift via `bash scripts/test_all.sh` (ALL GREEN, byte-identical to auditor 2026-05-06 00:05); no count drift, but the citation note in the table caption was left at "auditor 2026-05-04 00:05" since the DoD only requires update if drifted -- planner-curation nit for the next planner pass. Phase 3 status cell in the same table row still reads "OPEN -- 0.6.1/0.6.2 uncommitted"; left unchanged because the strict DoD targeted only the version cell -- second planner-curation nit for the next planner pass. Closes the only next-pull-ready item promoted by planner 2026-05-06 07:10; next-pull-ready set is now empty again -- 9am-12pm chain hits empty-queue per the worker-11am 2026-04-30 NEEDS-INPUT precedent unless Eric `[x]`-approves at least one PROPOSED item during evening review.
  - Commit: `worker-8am: P2 ROADMAP refresh post-261a674 (QUEUE 3.8)` -- bookkeeping commit on `ROADMAP.md`, `QUEUE.md`, `DONE.md`, `JOURNAL.md`. `.git/index` is structurally corrupt (auditor 2026-05-06 P1 + researcher 2026-05-06 04:00 validation -- bad index sha1 signature, three phantom UU entries with control-character paths); used the established plumbing-path workaround (`GIT_INDEX_FILE=/tmp/...` + `git read-tree HEAD` + `git add` + `git write-tree` + `git commit-tree` + direct ref-write to `.git/refs/heads/main`) per worker-9am 2026-04-29 procedure + `roles/worker.md` step 5. Local commit only; remote push deferred per CLAUDE.md push policy (Eric pushes nightly).
  - Tests: pass -- `bash scripts/test_all.sh` ALL GREEN, 702 tests across 4 tools (606 + 40 + 30 + 26). No regression vs. auditor 2026-05-06 00:05 baseline. Note: change is doc-only (no `.py` touched, no spec touched, no source touched), but DoD bullet "test counts updated if drifted" required a fresh count before deciding whether to edit; counts confirmed unchanged.
  - Skill: `engineering:code-review` listed in the QUEUE entry but not invoked -- `engineering:*` skills are not in this run's available-skills list (anthropic-skills:* + commands only); per `roles/worker.md` "If a skill is unavailable in this run, note that in `JOURNAL.md` and fall back to the prior bare-handed behavior" -- noting here, fell back to manual diff inspection (3 textual changes; no truncation; no NUL bytes; clean trailing newline; revision-history append byte-for-byte matches the planned content).
  - Hazard pre-empted: Edit-tool size-cap-in-place tail-drop hazard (worker-8am 2026-05-05 08:15 + worker-9am 2026-05-05 09:00). Did NOT use Edit/Write on `ROADMAP.md`, `QUEUE.md`, or `DONE.md` -- all three edits done via Python binary-mode read+write through bash. Pre/post byte counts match the planned deltas exactly (ROADMAP.md +475b for 2 string replacements + 1 append; QUEUE.md -1354b for 3.8 block removal in next-pull-ready section + cross-listed Phase 3 block; DONE.md += this entry). 0 NUL on all three.


## 2026-05-12

- [P1] Execute the orphan-dir tracked-vs-ignored decision -- doc-alignment lift (QUEUE 3.7, [phase-3] [arch], option A) -- done by process-tools-worker-8am at 08:00. Outcome: closed the Phase-3 doc-alignment lift triggered by Eric's 2026-05-11 cowork-session option-A pick (keep `compliance-matrix/`, `nimbus-skeleton/`, `process-tools-common/` tracked as proper packages, effective 2026-05-05; recorded in `DECISIONS-orphan-dirs.md` top blockquote). Three files cleaned up; read-only off-limits constraint lifted. **CLAUDE.md**: (1) removed the off-limits bullet for the three orphan dirs (line 18 in pre-state); (2) tightened the BPMN/Camunda task-specific waiver -- dropped the now-redundant "Read-only against orphan-dirs ... still applies" reference while keeping the doc-vs-source scoping (emitter artifacts -> scratch dir; findings -> `nimbus-skeleton/DECISIONS.md`, doc not source); (3) updated the project-specific note (line 37 in pre-state) to record the resolution + this run's closure ("QUEUE 3.7 doc-alignment closed by worker-8am 2026-05-12; the prior read-only off-limits constraint is lifted"). **ACTION_ITEMS.md** Phase-0 stale framing: (1) the "Biggest concern surfaced" prose got an inline HISTORICAL/RESOLVED corrective marker; (2) the "Morning TODO" #1 "decide git tracking" line got struck-through with the resolution note. **COMMIT_PLAN.md**: added a top-of-file HISTORICAL status banner clarifying that the nine commits in the plan were fully executed in 2026-04-25/26 and the document is retained as record-keeping; reworded the "Execute on your end" prose to flag it as 2026-04-25 state-of-play. Byte deltas: CLAUDE.md 6546 -> 6223 / 87 -> 86 lines (the off-limits bullet's net removal); ACTION_ITEMS.md 13716 -> 14266 / 178 lines (+550b for 2 inline correctives); COMMIT_PLAN.md 12800 -> 13449 / 313 -> 316 lines (+649b for status banner). 0 NUL bytes on all three; clean trailing newlines verified. Closes the Phase-3 doc-alignment dependency for 2.1 / 2.3 / 2.4 / 4.2 / 4.3 / 4.4 / 4.6 (gating axis); unblocks the Phase-3 next-pull-ready set if Phase-2 items get promoted in evening review. First DONE entry in ~6 calendar days (the empty-DONE streak from 2026-05-07 through 2026-05-12 morning ends here).
  - Commit: `worker-8am: P1 QUEUE 3.7 orphan-dir doc-alignment lift -- option A closure (CLAUDE.md + ACTION_ITEMS.md + COMMIT_PLAN.md)` -- bookkeeping commit on `CLAUDE.md`, `ACTION_ITEMS.md`, `COMMIT_PLAN.md`, `QUEUE.md`, `DONE.md`, `JOURNAL.md`. `.git/index` is structurally stale + locked by a Windows-side process (auditor 2026-04-30 / 2026-05-06 / 2026-05-07 PROPOSED thread; mtime 2026-05-07 06:06:47 UTC unchanged for 5 days; lock 2026-05-07 06:07:44 UTC); used the established plumbing-path workaround per `roles/worker.md` step 5 + the worker-9am 2026-04-29 / worker-10am 2026-04-30 procedure (`GIT_INDEX_FILE=/tmp/...` + `git read-tree HEAD` + `git add` + `git write-tree` + `git commit-tree` + direct `printf <sha> > .git/refs/heads/main`). Local commit only; remote push deferred per CLAUDE.md push policy (Eric pushes nightly). Local HEAD will be 2-ahead of `origin/main` after this commit (Eric's `bdc9e04` from 2026-05-11 + this commit; expected push tonight).
  - Tests: n/a -- DoD is doc-only (CLAUDE.md / ACTION_ITEMS.md / COMMIT_PLAN.md; no `.py` touched, no spec touched, no source touched). Last green baseline is night-auditor 2026-05-12 ~00:05 (**703/703** across 4 tools: 607 requirements-extractor + 40 nimbus-skeleton + 30 compliance-matrix + 26 process-tools-common; up from the prior 702 baseline via Eric's `bdc9e04` 2026-05-11 0.6.2 follow-up). No regression possible -- no source touched this shift.
  - Skill: `engineering:system-design` (queue-entry-recommended) and `engineering:code-review` (worker-role-mandatory for code-touching items) BOTH unavailable in this run's available-skills list (anthropic-skills:* + commands only; no `engineering:*` skills); per `roles/worker.md` skill-fallback convention -- noted here + JOURNAL.md, fell back to manual diff review. Bare-handed review against `GIT_INDEX_FILE=/tmp/wkr_review.idx git diff HEAD --` on all three files: changes match the planned QUEUE 3.7 option-A DoD (off-limits removal in CLAUDE.md + stale-framing correctives in ACTION_ITEMS.md + historical status banner in COMMIT_PLAN.md); no edit-tool truncation patterns; 0 NUL bytes; all three files end on complete sentences + newlines; no abruptly cut-off content. Bare-handed review approves the diff.
  - Hazard pre-empted: Edit-tool size-cap-in-place tail-drop hazard (worker-8am 2026-05-05 08:15 + worker-9am 2026-05-05 09:00 + planner 2026-05-08 07:10; 3rd documented occurrence). Did NOT use Edit/Write on any of the three target files -- all six total edits (3 to CLAUDE.md + 2 to ACTION_ITEMS.md + 1 to COMMIT_PLAN.md) done via Python binary-mode read+write through bash with pre/post byte-count verification + tail-80 trailing-newline check + NUL-byte sweep. Pre/post byte counts match the planned deltas exactly on all three files.
  - Note: the planner 2026-05-12 07:10 framed 3.7 as "now doc-cleanup only" but did NOT promote it to next-pull-ready. The structural gating (Eric's A/B/C pick) was resolved 2026-05-11, so the item was pickable by walking the phase-decomposed list from the top -- 2.1/2.3/2.4 (3.7-dep), 2.5 (attended), 3.2/3.5/3.6 (PROPOSED `[x]` dep), then 3.7 itself as the first ungated item. This is the first non-bailout 8am Worker run in ~6 days; ends the 19-consecutive-bailout streak from 2026-05-06 through 2026-05-09 (the 2026-05-10/-11 pause days are uncounted).
