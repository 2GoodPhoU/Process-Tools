"""Nimbus Skeleton Mapper — turn DDE-extracted requirements into a starter
UML activity diagram (swimlanes, actions, sequence flows, decision gateways).

Output formats:

- **PlantUML** (``.puml``) — text-based UML activity diagram. Renders
  anywhere and is the fastest way to eyeball the skeleton.
- **XMI 2.5** (``.xmi``) — OMG UML 2.5 interchange format. Importable
  into Cameo, Enterprise Architect, MagicDraw, Papyrus, and most other
  serious UML tools.
"""

__version__ = "0.1.0"
__all__ = ["__version__"]
